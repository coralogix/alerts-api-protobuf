version: v1
deps:
  - buf.build/googleapis/googleapis
lint:
  use:
    - DEFAULT
  ignore_only:
    ENUM_ZERO_VALUE_SUFFIX:
      - FlowOperator
    ENUM_VALUE_PREFIX:
      - FlowOperator
    FIELD_LOWER_SNAKE_CASE:
      - nextOp
breaking:
  use:
    - WIRE
