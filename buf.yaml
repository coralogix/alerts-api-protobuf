version: v1
deps:
  - buf.build/googleapis/googleapis
lint:
  use:
    - DEFAULT
  ignore_only:
    ENUM_ZERO_VALUE_SUFFIX:
      - FlowOperator
    ENUM_VALUE_PREFIX:
      - FlowOperator
breaking:
  use:
    - WIRE
